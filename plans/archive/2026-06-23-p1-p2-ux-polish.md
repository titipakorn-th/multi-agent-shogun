---
title: P1 + P2 UX Polish Implementation (cmd_037 §8.8 backlog continuation)
auto_continue: true
project: lotuss
---

# Plan: P1 + P2 UX Polish Implementation

## Status

- [x] Task 1: cmd_046 — Designer drafts §10 (P1 + P2 implementation spec for all 12 items)
- [x] Task 2: cmd_047 — Fixer P1-01, P1-02, P1-04, P1-05 (copy-only, smallest surface)
- [x] Task 3: cmd_048 — Fixer P1-07 + P2-01, P2-02, P2-03 (tooltips + PDPA badge)
- [x] Task 4: cmd_049 — Fixer P1-06 (audit-report help page) + P1-08 (B2B second-approver audit-log modal)
- [x] Task 5: cmd_050 — Fixer P1-03 (trust-score delta line chart + new /recipients/:id/analytics/history endpoint)
- [x] Task 6: cmd_051 — Oracle + Observer docs review
- [x] Task 7: cmd_052 — Commit + push + design.md §8.10 append + Lord-facing Telegram

## Problem

cmd_037 produced the full UX audit + 16-item P0/P1/P2 backlog. cmd_040–cmd_044
shipped P0 (4 critical UX gaps). Lord approved the full P1+P2 path — 12 items
remaining, all addressing trust signals, copy clarity, tooltip affordances,
and audit-log admin UX. cmd_037 §8.8 lists all 12; this plan implements them.

## Design

### Scope (per cmd_037 §8.8)

**P1 — 8 items** (important, ship in next sprint):

| # | Item | Surface | Pain | Dependency |
|---|------|---------|------|------------|
| P1-01 | Onboarding "skip tour" placement — move to header right of step indicator | §7.2 | P-01 | cmd_035b scope expansion |
| P1-02 | "Seller guaranteed" copy clarification on §7.2 slide B | §7.2 slide B | P-02 | Copy change |
| P1-03 | Trust-score delta line chart with "why this changed" annotations (B-05 borrow) | §2.4 | P-06 | New GET `/recipients/:id/analytics/history` endpoint |
| P1-04 | Counter-evidence panel inline help (Thailand Post drop-off receipt format) | §2.2 Step 4 | P-07 | Copy change |
| P1-05 | SLA auto-release countdown in plain Thai with CTA pair (B-02 borrow) | §2.1 Step 4 | P-09 | Existing component extension |
| P1-06 | Audit-report per-section "what does degraded mean?" link | /admin §3.1 | P-10 | Documentation page |
| P1-07 | PDPA §28 Bangkok ap-southeast-3 jurisdictional microbadge on first-visit footer | site-wide | P-14 | cmd_030 jurisdiction copy reuse |
| P1-08 | B2B second-approver name in audit-log preview modal (Q3 option b) | /admin | P-13 | Audit-log preview modal new component |

**P2 — 4 items** (defer-beyond-pilot polish):

| # | Item | Surface | Pain | Dependency |
|---|------|---------|------|------------|
| P2-01 | §6.2 "buyer email" inline help ("Auto-notify buyer when charge is paid") | §6.2 | P-04 | Tooltip component |
| P2-02 | Three-tier balance `total` and `transferable` first-hover tooltips | §2.3 | P-05 | Existing tooltip extension |
| P2-03 | Sandbox tray "What is this?" tooltip on gateway | §5.5 | P-15 | Tooltip component |
| P2-04 | Trust-tier copy polish ("เร็วกว่าปกติ 3 วัน / Faster by 3 days") (B-06 borrow) | §2.3 | n/a (polish) | Copy change |

### Grouping rationale

| Group | Items | Rationale |
|-------|-------|-----------|
| Task 2 (cmd_047) | P1-01, P1-02, P1-04, P1-05 | Copy-only + 1 component extension. Smallest surface, lowest risk, no new endpoints. |
| Task 3 (cmd_048) | P1-07, P2-01, P2-02, P2-03 | Tooltip components + site-wide PDPA badge. Reusable component pattern. |
| Task 4 (cmd_049) | P1-06, P1-08 | Admin-only /admin surfaces. Audit-log modal is a new component but contained. |
| Task 5 (cmd_050) | P1-03 | Backend endpoint + frontend chart. Largest single item (backend + frontend + chart lib). |

### File territory (cumulative, RACE-001 style — non-overlapping)

- `design.md §10` (NEW additive, doesn't touch §1–§9)
- `frontend/src/routes/+page.svelte` (P1-04, P1-05, P2-02)
- `frontend/src/routes/onboarding/+page.svelte` (P1-01, P1-02)
- `frontend/src/routes/admin/+page.svelte` (P1-06, P1-08)
- `frontend/src/components/Tooltip.svelte` (NEW for P2-01/02/03; reuse across P1-07 variant)
- `frontend/src/components/AuditLogModal.svelte` (NEW for P1-08)
- `frontend/src/components/TrustScoreChart.svelte` (NEW for P1-03)
- `frontend/src/components/Footer.svelte` (P1-07 site-wide)
- `backend/src/api/analytics.rs` (NEW endpoint for P1-03: GET /recipients/:id/analytics/history)
- `tests/unit/*.bats` (P1-03 endpoint regression tests)

### Quality bar (inherited from cmd_040 plan, tightened)

- cmd_027 v2 calm trust-forward tokens (no new palette)
- Thai-first bilingual copy on every user-facing string
- WCAG 2.1 AA contrast (≥4.5:1) verified on tooltip text and PDPA badge
- 44x44px touch targets on skip-tour header button (P1-01)
- vite build clean; svelte-check 0 errors (cumulative)
- cargo test 106+/0/0 (P1-03 endpoint regression tests added; net +5 expected)
- Oracle round 1/4 minimum (each task)
- Ponytail: ≤80 lines of new Svelte component code per item; extract only when reused ≥2 places

## Task Details

### Task 1: cmd_046 — Designer drafts §10 (P1 + P2 implementation spec)

Designer reads cmd_037 §8.5 + §8.7 + §8.8 and produces implementation-grade
spec for all 12 items. File: `design.md §10` (NEW additive). Cover:

- §10.1: P1-01 onboarding skip-tour placement (header right of step indicator, 44x44 touch target)
- §10.2: P1-02 seller-guaranteed copy (Thai-first bilingual, §7.2 slide B)
- §10.3: P1-03 trust-score delta line chart (data shape: `{ date, score, delta_reason }[]`; component sketch)
- §10.4: P1-04 counter-evidence panel inline help (Thailand Post drop-off receipt format, Thai)
- §10.5: P1-05 SLA auto-release countdown (plain Thai "ปล่อยอัตโนมัติใน 2 วัน 4 ชั่วโมง / Auto-release in 2d 4h" + CTA pair)
- §10.6: P1-06 audit-report "what does degraded mean?" link + help page route `/admin/help/audit-degraded`
- §10.7: P1-07 PDPA §28 Bangkok microbadge (`🇹🇭 กรุงเทพฯ · PDPA §28` tooltip)
- §10.8: P1-08 B2B second-approver audit-log preview modal (new component, name + decision context)
- §10.9: P2-01 "buyer email" inline help tooltip
- §10.10: P2-02 balance tier tooltips (`total` = locked + transferable + pending; `transferable` = available now)
- §10.11: P2-03 sandbox tray "What is this?" tooltip
- §10.12: P2-04 trust-tier copy polish
- §10.13: Tooltip component spec (shared, reusable)
- §10.14: P1-03 endpoint response schema: `GET /recipients/:id/analytics/history` → `{ series: [{ date, score, delta_reason }] }`

Acceptance: §10 renders cleanly with all 12 items specified; Oracle 1/4 docs review approves.

### Task 2: cmd_047 — Fixer P1-01 + P1-02 + P1-04 + P1-05

Single-pass copy + 1 component extension:

- P1-01: Move "skip tour" button from §7.2 step-1 only to header right of step indicator (all 5 steps). 44x44px touch target.
- P1-02: Update §7.2 slide B copy per spec.
- P1-04: Add inline help text under counter-evidence file-upload component.
- P1-05: Extend existing SLA countdown component with plain Thai text + CTA pair.

File territory: `frontend/src/routes/onboarding/+page.svelte`, `frontend/src/routes/+page.svelte` (P1-04, P1-05).

Acceptance: vite build clean; svelte-check 0 errors; no backend changes.

### Task 3: cmd_048 — Fixer P1-07 + P2-01/02/03

Tooltip component + PDPA badge:

- Extract `<Tooltip>` shared component (Ponytail: extract only when used ≥2 places; tooltips used in 4+ places qualifies)
- P1-07: Site-wide footer microbadge with PDPA §28 tooltip
- P2-01: Tooltip on §6.2 buyer-email field
- P2-02: Tooltips on §2.3 balance `total` and `transferable` fields
- P2-03: Tooltip on §5.5 sandbox tray gateway label

File territory: `frontend/src/components/Tooltip.svelte` (NEW), `frontend/src/components/Footer.svelte`, `frontend/src/routes/+page.svelte`, frontend surfaces for §5.5/§6.2.

Acceptance: vite build clean; svelte-check 0 errors; WCAG verified on tooltip text contrast.

### Task 4: cmd_049 — Fixer P1-06 + P1-08

Admin-only audit-report UX:

- P1-06: `/admin/help/audit-degraded` page (Markdown rendered). Add "?" link next to each audit-report section that opens this page (or scrolls to anchor).
- P1-08: New `<AuditLogModal>` component. Triggered by clicking an audit-log entry in /admin. Shows: actor, action, timestamp, second-approver name + decision context.

File territory: `frontend/src/routes/admin/+page.svelte`, `frontend/src/routes/admin/help/audit-degraded/+page.svelte` (NEW), `frontend/src/components/AuditLogModal.svelte` (NEW).

Acceptance: vite build clean; svelte-check 0 errors; no backend changes.

### Task 5: cmd_050 — Fixer P1-03 (backend + frontend + chart)

Largest single item — backend endpoint, frontend chart component, B2B borrower pattern:

- Backend: `GET /recipients/:id/analytics/history` in `backend/src/api/analytics.rs`. Admin-key gated OR recipient-self (per existing auth pattern). Response: `{ series: [{ date, score, delta_reason }] }`. 5 #[serial] regression tests (empty / single-day / 30-day / >30-day truncation / auth).
- Frontend: `<TrustScoreChart>` line chart component using `<svg>` (Ponytail: no chart lib — native SVG line + dots). Loads from new endpoint on mount. "Why this changed" annotation overlay on data points where `delta_reason != null`.
- Wire into recipient dashboard §2.4.

File territory: `backend/src/api/analytics.rs` (NEW), `backend/src/api/mod.rs` (1-line route reg), `frontend/src/components/TrustScoreChart.svelte` (NEW), `frontend/src/routes/dashboard/+page.svelte` (P1-03 wiring).

Acceptance: cargo test 110+/0/0 (5 new tests); vite build clean; svelte-check 0 errors; WCAG on chart text.

### Task 6: cmd_051 — Oracle + Observer docs review

Oracle 1/4 round review of cmd_047/048/049/050:

- Verify cmd_027 v2 tokens preserved (no new palette)
- Verify Thai-first bilingual on every new string (12 items × 2 langs = 24 strings)
- Verify WCAG 2.1 AA on tooltip text, PDPA badge, chart annotations
- Verify Ponytail rules followed (Tooltip extracted once used ≥2 places; chart is native SVG no-lib)
- Verify file isolation (no cross-imports with cmd_040/041/042/043/044)

Observer E2E verification: **SKIPPED per Lord directive (cmd_043 precedent).** cargo test + vite build + svelte-check are sufficient.

Acceptance: Oracle verdict=approved, 0 critical/high/medium/low findings.

### Task 7: cmd_052 — Commit + push + changelog + Lord Telegram

- Commit P1+P2 work (cmd_047/048/049/050)
- Push to origin/main
- Append `design.md §8.10 changelog` with P1+P2 implementation summary
- Update `design_phase_1_plan.md §3.5` with P1+P2-done rows
- Write `docs/superpowers/specs/2026-06-23-p1-p2-ux-polish.md` (Lord-facing spec doc)
- Mark all 7 tasks `[x]` in this plan
- Send strategic Telegram: "🏯 P1+P2 complete. 12 UX polish items shipped. SafePay UI surface is now cmd_037-aligned."

Acceptance: working tree clean, origin/main ahead of local, plan marked complete.

## Dependencies

- cmd_044 (P0 commit+push) must close before Task 2 starts (no file overlap, sequential for clarity)
- cmd_046 (designer §10) must close before Tasks 2-5 start (Oracle-approved spec is the spec source)
- cmd_047-050 are file-isolated from each other (different surfaces) — can run sequentially or parallel

## Estimated Effort

- Task 1 (designer §10): ~45-60 min (12 items, more than P0)
- Task 2 (P1-01/02/04/05): ~30-45 min (copy + 1 extension)
- Task 3 (tooltips + PDPA badge): ~45-60 min (extract component + 4 placements)
- Task 4 (audit-report help + modal): ~30-45 min (new page + new component)
- Task 5 (P1-03 backend + chart): ~60-90 min (largest, includes new endpoint + native SVG chart)
- Task 6 (Oracle review): ~20-30 min
- Task 7 (commit + push): ~15 min
- Total: ~4.5-6 hours wall-clock

## Rollback

- Each implementation cmd is independently revertible via `git revert <commit>`
- §10 spec is additive; reverting implementation leaves §10 intact for future retry
- Pre-deploy: SafePay is in pilot-pending state; rollback is a non-event
- If P1-03 chart component (Task 5) introduces regressions, can ship P1+P2 without it and defer P1-03 to next sprint